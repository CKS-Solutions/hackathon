# Copy to infra/.env and adjust if needed.
# Used by: docker compose -f infra/docker-compose.yml

POSTGRES_USER=dev
POSTGRES_PASSWORD=dev
POSTGRES_DB=video_system

# RabbitMQ (optional: image defaults are guest/guest for management UI on port 15672)
