apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# Base uses ms-stub:local and imagePullPolicy: IfNotPresent; load image into cluster before apply (kind load / minikube image load)
